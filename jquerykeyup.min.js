$(document).on("keyup", "INPUT SELECTOR", function () {
    var a=$(this),b=a.b(),c=a.data("timeout");
    function d(){
        // Callback Action
    }
    c&&(clearTimeout(c),a.a("timeout"));0==b||""==b||null==b||a.data("timeout",setTimeout(d,400));
})